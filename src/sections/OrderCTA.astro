---
import Container from '../components/Container.astro';
import Button from '../components/Button.astro';
---

<section class="py-16 lg:py-20 bg-pure-cream">
  <Container>
    <div class="max-w-xl mx-auto text-center space-y-6">
      <h2 class="font-display text-3xl sm:text-4xl font-semibold text-charred-brown">
        Hungry?
      </h2>
      <p class="text-earth-brown">
        WhatsApp us — for table holds, larger group orders, or to ask if the lamb ribs are ready.
      </p>
      <div class="flex flex-col sm:flex-row items-center justify-center gap-4 pt-2">
        <Button href={`https://wa.me/${import.meta.env.PUBLIC_WHATSAPP_NUMBER || '601111939872'}`} variant="primary">
          WhatsApp Us
        </Button>
        <Button href="/story#visit" variant="outline">
          Visit Us
        </Button>
      </div>
    </div>
  </Container>
</section>
