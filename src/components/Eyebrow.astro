---
interface Props {
  class?: string;
}

const { class: className = '' } = Astro.props;
---

<span class={`inline-block text-xs font-semibold uppercase tracking-[0.2em] text-thai-chili-red ${className}`}>
  <slot />
</span>
