---
interface Props {
  href?: string;
  variant?: 'primary' | 'outline' | 'ghost';
  class?: string;
}

const { href, variant = 'primary', class: className = '' } = Astro.props;

const baseStyles = 'inline-flex items-center justify-center px-6 py-3 text-sm font-medium rounded-full transition-[colors,transform,box-shadow] duration-300 ease-[cubic-bezier(0.23,1,0.32,1)] active:scale-[0.98]';

const variants = {
  primary: 'bg-thai-chili-red text-white hover:bg-thai-chili-red/90 hover:shadow-lg hover:shadow-thai-chili-red/20',
  outline: 'border-2 border-thai-chili-red text-thai-chili-red hover:bg-thai-chili-red hover:text-white hover:shadow-lg hover:shadow-thai-chili-red/15',
  ghost: 'text-dusty-slate hover:text-deep-indigo hover:bg-deep-indigo/5',
};

const classes = `${baseStyles} ${variants[variant]} ${className}`;
---

{href ? (
  <a href={href} class={classes}>
    <slot />
  </a>
) : (
  <button type="button" class={classes}>
    <slot />
  </button>
)}
